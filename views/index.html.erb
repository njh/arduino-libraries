<p>
  Below is a list of all the <em><%= libraries.keys.count %></em>
  libraries registered in the Arduino Library Manager.
</p>

<ul>
  <% libraries.keys.sort.each do |key| %>
    <% library = libraries[key] %>
    <li><a href="./libraries/<%= key %>"><%= library[:name] %></a></li>
  <% end %>
</ul>
